# CytoStudio Build Audit

This audit maps the current prototype to the requested prompt sequence. The app is now a runnable local-first product prototype, not merely the original blueprint catalog. Science-heavy and regulatory capabilities are represented as UI/state scaffolds until real reference data, engines, and validation suites are added.

## Current evidence

- Git repository initialized and committed: `b599ea8 Initial CytoStudio prototype`.
- App entrypoint: `index.html`.
- Application logic: `app.js`.
- Design system: `styles.css`.
- Original blueprint preserved: `blueprint-catalog.html`, `CytoStudio-Blueprint-and-Codex-Prompts.md`, and `prompts/`.
- Visual concept asset: `assets/design/cytostudio-workspace-concept.png`.
- FCS foundation: `fcs-core.js` plus `tests/fcs-core.test.cjs`.
- Worker import adapter: `fcs-import-worker.js`.
- Large-file import boundary: `docs/fcs-large-file-strategy.md`.
- Public FCS fixture provenance: `tests/fixtures/public/flowWorkspaceData/manifest.json`.
- Verification performed: JavaScript syntax check, local HTTP 200 check, Playwright desktop/mobile screenshots, Command-K check, tab-surface checks, and mobile overflow check.

## Prompt-by-prompt status

| Prompt | Status | Evidence / current behavior |
|---|---|---|
| 00 Vision / guardrails | Prototype complete | Three-pane app shell, state store, README architecture choice, light/dark support. Tauri/Rust/Arrow are documented next steps. |
| 01 Shell / navigation / design system | Prototype complete | Left sample rail, population tree, center canvas, inspector, toolbar, status bar, Command-K, responsive layout, persisted theme. Panel resizing/collapse is only lightly scaffolded. |
| 02 FCS import / experiment manager | Prototype partial | Drag-drop and file-picker imports now parse FCS 3.0/3.1 header/TEXT metadata, parameter labels, common numeric event data, keyword counts, sampled events, and spillover through `fcs-core.js`, with `fcs-import-worker.js` moving served-local parsing off the UI thread, streamed byte-level progress/cancel state, event-level parse progress, direct parser fallback when workers are unavailable, metadata-only preflight for oversized files, and sample-to-sample drag-drop grouping in the experiment browser. Fixture tests cover escaped delimiters, metadata-only parsing, max-event limits, parser progress callbacks, transforms, spillover, big-endian integer data, 8/16/32-bit integer widths, and a public Bioconductor `flowWorkspaceData` FCS3.0 DIVA fixture. Production memory mapping and broader public/vendor fixture coverage remain. |
| 03 Transforms / scaling | Prototype partial | Linear, log, arcsinh, logicle-like, and biexponential-alias transforms now live in shared tested code, expose per-axis floor/width/cofactor controls, auto-tune defaults from active parameter ranges/event previews, drive scatter/density normalization, render scale-aware axis ticks, and bin histograms from active event data. Full numerical parity with `flowCore`, publication-grade tick semantics, and formal reference validation remain. |
| 04 Plot canvas | Prototype complete | Canvas scatter, density, histogram, UMAP-style plots, legends, overlays, zoom gesture capture, publication-style chrome. GPU/datashader-scale rendering is not yet implemented. |
| 05 Manual gating | Prototype complete for local prototype | Toolbar tools, direct plot drawing for rectangle/ellipse/quadrant/interval/lasso gates, click-to-place polygon vertices with Enter/double-click close, selected-gate handles, direct drag editing for corners/vertices/crosshairs/interval edges, keyboard nudge, inspector rename/recolor, colored gate overlays/handles, creation fill animation, computed AND/OR/NOT boolean gates from existing gate membership, labels, hierarchy updates, and normalized gate geometry evaluation. Production-scale recomputation and reference interop validation remain outside this prompt. |
| 06 Hierarchy / live-linking / backgating | Prototype complete for local prototype | Gate tree, child population creation, recursive parent-filtered population counts, sample-aware stats, selection-linked inspector/table updates, live gate-label refresh, reparenting by tree drag, and visual backgate overlays/rug marks in ancestor plots are implemented for sampled events. True million-event incremental recomputation remains engine work. |
| 07 Statistics / table | Prototype complete for local prototype | Inspector and table statistics compute from gated event subsets: count, % parent, % total, median, mean, geometric mean, robust CV, percentiles, configurable statistic columns, drag/drop population rows, samples-as-rows pivoting, sortable headers, CSV export, Excel-compatible proof export, clipboard copy, and a CD4/CD8 derived parameter. Native binary Excel export remains production work. |
| 08 Compensation / spillover QC | Prototype complete for local prototype | Compensation matrix state parses embedded spillover keywords when present, matrix edits apply to the active compensated event stream, sample/group/workspace matrix assignment is explicit, import and single-stain-fit proof workflows create reviewable matrices, plots/statistics/gates refresh from compensated values, and the N-by-N QC grid plots real compensated channel pairs. Auto-comp and control fitting remain reviewable estimates, not validated single-stain algorithms. |
| 09 Spectral unmixing | Prototype complete for local prototype | Spectral detector proof loading, reference signature import/build workflows, autofluorescence modeling, signature-quality warnings, least-squares-style review metadata, residual parameter generation, and reviewable unmixing create named unmixed fluorophore parameters available to plots, gates, and statistics. This remains a normalized-signature prototype, not validated NNLS on raw spectral vendor data. |
| 10 Templates / batch | Prototype complete for local prototype | The Batch view saves the active hierarchy as a reusable template, exports/imports template JSON proof artifacts, applies templates to a selectable sample group, pages through linked samples, stores sample-specific gate overrides with tailored badges, renders small-multiple gate review tiles, and recomputes/export combined group statistics into the table editor. High-volume recomputation and native template-file management remain production work. |
| 11 High-dimensional / cluster explorer | Prototype complete for local prototype | The High-Dim view supports parameter selection, UMAP/t-SNE mode, FlowSOM/graph-style cluster assignment, worker-backed progress/cancel state with fallback, marker coloring, sampled embedding rendering, cluster selection, cluster-by-marker heatmap, sample comparison history, reproducible seed reporting, and backgating a selected cluster into the population hierarchy. These remain deterministic local approximations, not validated UMAP/t-SNE/FlowSOM or graph algorithms. |
| 12 Figure editor | Prototype complete for local prototype | The Figure view is now a page-canvas editor with positioned live plot tiles, text labels, arrows, picture-in-picture insets, layer selection, direct drag/resize handles, nudge/resize, snap/alignment/distribution controls, appearance controls for font/gate/tick style, reusable template save state, browser-native SVG/PNG/TIFF/PDF proof exports, and selected-plot package export. Print-grade PDF/TIFF/vector export and color management remain production work. |
| 13 Pipeline / reports / export | Prototype complete for local prototype | The Pipeline view now has a structured typed action record model, step-through timeline, event details, target-sample replay preview that reapplies population ratios, report preview and HTML/PDF-proof generation with hierarchy/statistics/pipeline content, structured pipeline JSON export, gated FCS proof export, event CSV/Parquet proof export, statistics Excel-compatible export, and GatingML proof export with dimensions and geometry metadata. Binary FCS/Parquet/Excel writers, validated PDF generation, full undo/redo state restoration, and formal GatingML conformance remain production work. |
| 14 Collaboration / workspace / interop | Prototype complete for local prototype | The Share view now defines a v2 workspace JSON package with sample path/hash references, population counts, gates, plots, templates, figure settings, scientific settings, structured pipeline records, replay preview, and workspace import/sync metadata; supports browser open/save, autosave/recovery, self-contained share-package export without raw FCS duplication, shared-location handoff simulation with version/conflict/change-log status, structured audit-log review, GatingML export/import proof, and FlowJo/Cytobank migration proof actions that create imported template records. Native desktop dialogs, real file locks, multi-user merge conflict resolution, real shared-location sync, and production interop parsers remain production work. |
| 15 Onboarding / accessibility / polish | Prototype complete for local prototype | First-run onboarding now offers bundled conventional and spectral sample launch paths, guided coach marks for gate -> population -> statistics -> pipeline, expanded keyboard shortcuts for search/gate/statistics/batch/high-dim/figure/pipeline/share/high-contrast, Command-K help items, high-contrast mode, non-color sample/population markers, gate dash patterns, screen-reader status updates, ARIA-labeled plots/rails/status surfaces, clearer import errors, large-sample readiness status, and responsive coach-mark behavior. Formal VoiceOver lab QA, certified contrast review, and production large-file memory profiling remain production work. |
| 16 Clinical / compliance | Prototype complete for local prototype | Clinical mode remains off by default and isolated from research workflows; when enabled it provides local authentication/role proof, a user registry, retention/access configuration, user/role/timestamp-stamped structured audit records with immutable-style hashes, locked/finalized analysis state with versioning for later changes, electronic signature records with reason/version/audit hash, compliance export proof with shared-responsibility language, and explicit no-validation/no-legal-compliance warnings. Production identity provider integration, validated signatures, retention enforcement, 21 CFR Part 11 validation, and lab SOP/legal review remain production work. |

## Next best slice

The best next build slice is to continue hardening Prompt 02 with broader public/vendor FCS fixture coverage or move to the next highest-impact production gap: validated transform parity in Prompt 03.
